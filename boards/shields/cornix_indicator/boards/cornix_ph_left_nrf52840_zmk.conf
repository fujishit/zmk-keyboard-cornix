# Left half in dongle mode (split *peripheral*).  Same physical layout as
# cornix_left, but the connectivity LED shows the link to the dongle instead
# of the BLE profile, because a peripheral has no host profile of its own.
CONFIG_RGBLED_WIDGET_CONN_LED_INDEX=0
CONFIG_RGBLED_WIDGET_BATTERY_LED_INDEX=1
CONFIG_RGBLED_WIDGET_LAYER_LED_INDEX=2
